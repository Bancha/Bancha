language: php

php:
  - 5.3

env:
  - DB=mysql

before_script:
  - mkdir .Bancha
  - mv ./* .Bancha/
  - mv .Bancha Bancha
  - sh -c "if [ '$DB' = 'mysql' ]; then mysql -e 'CREATE DATABASE cakephp_test;'; fi"
  - wget https://github.com/cakephp/cakephp/tarball/master -O cake.tar.gz
  - tar xf cake.tar.gz
  - mv cakephp-cakephp-* ./cakephp
  - chmod -R 777 ./cakephp/app/tmp
  - git clone https://github.com/Bancha/BanchaTestProject.git
  - cp -r BanchaTestProject/* cakephp/app/ 
  - echo "<?php
    class DATABASE_CONFIG {
    public \$default = array(
      'driver' => 'mysql',
      'persistent' => false,
      'host' => '0.0.0.0',
      'login' => 'root',
      'password' => '',
      'database' => 'cakephp_test',
      'prefix' => ''
    );
    public \$test = array(
      'driver' => 'mysql',
      'persistent' => false,
      'host' => '0.0.0.0',
      'login' => 'root',
      'password' => '',
      'database' => 'cakephp_test',
      'prefix' => '',
      'encoding' => 'utf8'
    );
    }" > cakephp/app/Config/database.php  
  - mv ./Bancha ./cakephp/app/Plugin/
  - cd ./cakephp


script:
  - chmod a+x ./lib/Cake/Console/cake
  - ./lib/Cake/Console/cake test Bancha AllTests
